package bonk

// func AllowYara(pid string, yaraRules []string) bool {
// 	// use the pid to get the executable binary using /proc/<pid>/exe
// 	// and then use the yara rules to see if it matches

// golang function that reads in yara rules and returns a list of matching rules
